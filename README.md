# Blinded

## Required Python Libraries

- pynput
- pygetwindow
- pywin32
- keyboard
- mouse

Use commands:
pip install pywin32
pip install pynput
pip install keyboard
pip install mouse
pip install pygetwindow
and any other modules required.

Please download the WHOLE zip file then extract.
Run 'main.bat' by double clicking it in your folders to start.